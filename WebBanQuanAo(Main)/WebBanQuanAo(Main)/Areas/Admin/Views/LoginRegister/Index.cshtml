@model WebBanQuanAo_Main_.Models.AdminUser

@{
    ViewBag.Title = "Đăng Nhập";
    Layout = "~/Areas/Admin/Views/Shared/_MasterLayout.cshtml";
}

@*<link href="~/Content/Admin/LoginRegister.css" rel="stylesheet" />

    <div class="auth-container">
        <h2>ĐĂNG NHẬP</h2>

        @using (Html.BeginForm("Login", "LoginRegister", FormMethod.Post, new { @class = "login-form" }))
        {
            @Html.AntiForgeryToken()

            if (ViewBag.ErrLogin != null)
            {
                <div class="alert alert-danger validation-message" role="alert" style="text-align: center;">
                    @ViewBag.ErrLogin
                </div>
            }

            <div class="form-group">
                <label for="Tên" class="d-block text-left small fw-bold">Tên</label>
                @Html.EditorFor(m => m.NameUser, new { htmlAttributes = new { @class = "form-control form-control-lg", placeholder = "Tên" } })
                @Html.ValidationMessageFor(m => m.NameUser, "", new { @class = "validation-message" })
            </div>

            <div class="form-group">
                <label for="PasswordUser" class="d-block text-left small fw-bold">Mật khẩu</label>
                @Html.PasswordFor(m => m.PasswordUser, new { @class = "form-control form-control-lg", placeholder = "Mật khẩu" })

                @Html.ValidationMessageFor(m => m.PasswordUser, "", new { @class = "validation-message" })
                @if (ViewBag.ErrLoginPass != null)
                {
                    @ViewBag.ErrLoginPass
                }
            </div>
            <div class="form-group">
                <input type="submit" value="ĐĂNG NHẬP" class="btn btn-dark-main" />
            </div>

            <div class="form-group">
                @Html.ActionLink("TIẾP TỤC ĐĂNG KÝ", "Register", null, new { @class = "btn btn-white-main" })
            </div>

            <div class="text-center mt-3">
                <a href="#" class="text-dark text-decoration-underline small">QUÊN MẬT KHẨU?</a>
            </div>
        }
    </div>*@
<h2>Đăng nhập</h2>
@using (Html.BeginForm("Login", "LoginRegister"))
{
    @Html.AntiForgeryToken()
    <table class="table">
        <tr>
            <td>
                @Html.LabelFor(m => m.ID)
            </td>
            <td>@Html.EditorFor(m => m.ID)</td>
            <td style="color:red;">
                @Html.ValidationMessageFor(m => m.ID)
                @if (ViewBag.ErrLoginID != null)
                {
                    @ViewBag.ErrLoginID
                }
            </td>
        </tr>
        <tr>
            <td>
                Tên đăng nhập
            </td>
            <td>@Html.EditorFor(m => m.NameUser)</td>
            <td style="color:red;">
                @Html.ValidationMessageFor(m => m.NameUser)
            </td>
        </tr>
        <tr>
            <td>
                Vai trò
            </td>
            <td>@Html.EditorFor(m => m.RoleUser)</td>
            <td style="color:red;">
                @Html.ValidationMessageFor(m => m.RoleUser)
            </td>
        </tr>
        <tr>
            <td>
                Mật khẩu
            </td>
            <td>@Html.PasswordFor(m => m.PasswordUser)</td>
            <td style="color:red;">
                @Html.ValidationMessageFor(m => m.PasswordUser)
                @if (ViewBag.ErrLoginPass != null)
                {
                    @ViewBag.ErrLoginPass
                }
            </td>
        </tr>
        <tr>
            <td>
                <input type="submit" value="Đăng nhập" class="btn btn-warning" />
            </td>
            <td>
                @Html.ActionLink("Đăng kí", "Register", null, new { @class = "btn btn-dark" })
            </td>
        </tr>
    </table>
}